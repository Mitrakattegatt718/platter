# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Rust / Tauri build output
src-tauri/target
src-tauri/gen/schemas

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Symlink to src-tauri/target/release/bundle
build

# Staged ffmpeg/ffprobe sidecars (see stage-ffmpeg.sh)
src-tauri/binaries

# An LGPL ffmpeg built inside the repo (docs/ffmpeg-build.md suggests a
# sibling directory, but a checkout-local build must not get committed)
ffmpeg-lgpl/

# Session-local Claude Code permissions; .claude/CLAUDE.md stays tracked
.claude/settings.local.json

# Secrets and machine-local config (signing identities, notarization profiles)
.env
.env.*

# TypeScript incremental build state
*.tsbuildinfo
